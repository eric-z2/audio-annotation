[project]
name = "audio-annotation"
version = "0.1.0"
description = "A Django project designed for audio based annotation"
readme = "README.md"
requires-python=">=3.12"
dependencies = [
    "django>=6.0.2,<6.1",
    "python-dotenv>=1.2.0"
]